name: flutter_mvvm
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1


environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2



dev_dependencies:
  flutter_test:
    sdk: flutter
  get_it:  
  provider:  
  shared_preferences:  
  firebase_core:  
  firebase_auth: 
  cloud_firestore:  
  http:  
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
